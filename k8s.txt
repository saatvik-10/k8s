kind create cluster --config clusters.yml --name local
kind delete cluster --name local

nginx-ingress-controller
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.12.1/deploy/static/provider/cloud/deploy.yaml

kubectl get nodes (running nodes)
kubectl get pods (logs)
kubectl get pods --v=8 (logs more verbose)
kubectl run <give_any_name> --image=<image_name> --port=<port_num> [not a proper way to start a pod]
kubectl apply -f <manifest_file>.yml [correct way]
kubectl get pods -w
kubectl get pods -f
kubectl get pods -owide
kubectl describe pod <name_given>
kubectl delete pod <name_given> 
kubectl get replicaset <rs_name>
kubectl get deployment <deployment_name>
kubectl rollout deployment/<deployment_name> --to-revision=<version_number>
kubectl scale deployment <deployment_name> --replicas <rs_num>
kubectl get pods -n <namespace_name>
kubectl delete pod nginx -n <namespace_name>
kubectl get configmaps

kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
kubectl top pod
kubectl top nodes

kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
kubectl port-forward svc/argocd-server -n argocd 8080:443
kubectl -n argocd get secret argocd-initial-admin-secret -o json

helm install <path_to_package>
helm list
helm uninstall <package_name>

Chart registries -> Bitnami, Artifacts Hub

labels: <label_name> -> one of the most important shit

nodeport range: 30000–32767

In the case of LoadBalancers, an external load balancer will be provisioned and you will be charged separately for that

Config Maps- non-confidential data in a key:value pair